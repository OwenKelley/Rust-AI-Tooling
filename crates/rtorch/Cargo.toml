[package]
name = "rtorch"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "PyTorch-like tensor + autograd API for Rust (1:1 mapping target)"

[dependencies]
rnumpy = { path = "../rnumpy" }
rpandas = { path = "../rpandas" }
